# Phase 2 Requirements - AI統合完全版

# Core FastAPI framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Data validation and settings
pydantic>=2.0.0
pydantic-settings>=2.0.0 

# Web and file handling
python-multipart>=0.0.6
jinja2>=3.1.2

# Google Sheets API integration
google-api-python-client>=2.100.0
google-auth>=2.22.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0

# === Phase 2: AI統合機能 ===

# OpenAI API integration
openai>=1.3.0              # OpenAI API client

# HTTP クライアント（AI APIコール用）
aiohttp>=3.8.0             # 非同期HTTPクライアント
httpx>=0.24.0              # Modern HTTP client

# データ処理とベクトル演算（将来のベクトル検索用）
numpy>=1.24.0              # 数値計算
scipy>=1.10.0              # 科学計算（類似度計算強化用）

# === 開発・テスト環境 ===

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Code quality tools
black>=23.0.0              # Code formatter
flake8>=6.0.0              # Linter
mypy>=1.5.0                # Type checking

# === Phase 3以降で使用予定（コメントアウト） ===

# Vector database (Phase 3)
# chromadb>=0.4.0          # ベクトルデータベース
# sentence-transformers>=2.2.0  # テキスト埋め込み

# データ操作とキャッシュ（必要に応じて）
# pandas>=2.0.0            # データ分析
# redis>=4.0.0             # 高速キャッシュ（オプション）

# ウェブスクレイピング（外部データソース用）
# beautifulsoup4>=4.12.0   # HTMLパース
# requests>=2.28.0         # HTTP requests

# PDF処理（Phase 3: マニュアル検索用）
# PyPDF2>=3.0.0           # PDF読み込み
# pdfplumber>=0.9.0       # PDF解析

# === 本番環境用（オプション） ===

# gunicorn>=21.2.0         # Production WSGI server
# prometheus-client>=0.16.0  # メトリクス収集

# === セキュリティ強化（本番用） ===

# slowapi>=0.1.8          # Rate limiting
# python-jose>=3.3.0      # JWT handling
# passlib>=1.7.4          # Password hashing

# === ログ・監視（本番用） ===

# structlog>=23.0.0       # 構造化ログ
# python-json-logger>=2.0.0  # JSONログ